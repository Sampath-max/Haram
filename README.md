hello project
create the project directory in linux
initailize git and readme
write the deploy.sh to ckeck the docker 
then build the images
after that remove the old container and create new container and create docker files
create dockerfile
commit and push
